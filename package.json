{
  "name": "rspress-jingju-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "license-report --only=prod,dev --output=json --fields=name --fields=installedVersion --fields=licenseType > src/components/DependenciesList/dependencies-licenses.json && rspress build",
    "dev": "rspress dev",
    "preview": "rspress preview",
    "netlify-deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@js-temporal/polyfill": "^0.5.1",
    "@rspress/core": "2.0.0-rc.3",
    "license-report": "^6.8.1",
    "rspress-plugin-reading-time": "^0.3.0"
  },
  "devDependencies": {
    "@rsbuild/plugin-image-compress": "^1.3.1",
    "@tailwindcss/postcss": "^4.1.18",
    "@tauri-apps/cli": "^2.9.6",
    "@types/node": "25.0.3",
    "netlify-cli": "^23.13.0",
    "tailwindcss": "^4.1.18"
  }
}